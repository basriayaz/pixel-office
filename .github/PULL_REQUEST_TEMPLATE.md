**What does this change?**

**How did you test it?** (`npm run typecheck`, started the office, which pages)

- [ ] Both `locales/en.json` and `locales/tr.json` updated if strings changed
- [ ] README updated if behaviour changed
